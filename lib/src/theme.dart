import 'package:flutter/material.dart';

class AppColors {
  static final scaffoldBackgroundColor = Colors.black;
  static const primaryColor = Color(0xFFa7e3fb);
  static const primaryTextColor = Colors.white;
  static const secondaryTextColor = Colors.grey;
  static const dangerColor = Colors.redAccent;
  static const successColor = Colors.greenAccent;
  static const cardBackgroundColor = Color(0xFF1a1a1a);

  static final List<Color> habitColors = [
    HSLColor.fromAHSL(1.0, 0.0, 0.7, 0.6).toColor(),
    HSLColor.fromAHSL(1.0, 30.0, 0.7, 0.6).toColor(),
    HSLColor.fromAHSL(1.0, 60.0, 0.7, 0.6).toColor(),
    HSLColor.fromAHSL(1.0, 90.0, 0.7, 0.6).toColor(),
    HSLColor.fromAHSL(1.0, 120.0, 0.7, 0.6).toColor(),
    HSLColor.fromAHSL(1.0, 150.0, 0.7, 0.6).toColor(),
    HSLColor.fromAHSL(1.0, 180.0, 0.7, 0.6).toColor(),
    HSLColor.fromAHSL(1.0, 210.0, 0.7, 0.6).toColor(),
    HSLColor.fromAHSL(1.0, 240.0, 0.7, 0.6).toColor(),
    HSLColor.fromAHSL(1.0, 270.0, 0.7, 0.6).toColor(),
    HSLColor.fromAHSL(1.0, 300.0, 0.7, 0.6).toColor(),
    HSLColor.fromAHSL(1.0, 330.0, 0.7, 0.6).toColor(),
  ];
}
